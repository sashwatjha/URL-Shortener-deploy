services:
  - type: web
    name: url-shortener-backend
    runtime: docker
    envVars:
      - key: PORT
        value: 8080
      - key: CORS_ORIGINS
        sync: false  # Set manually in Render dashboard
      - key: BASE_URL
        sync: false  # Set manually in Render dashboard